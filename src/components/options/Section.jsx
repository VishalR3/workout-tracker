const Section = ({ height = "150px" }) => {
  return (
    <div
      style={{
        width: "100%",
        height: height,
        borderRadius: "12px",
        backgroundColor: "#E6E6E6",
      }}
    ></div>
  );
};

export default Section;
